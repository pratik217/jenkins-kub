pipeline {
    agent {
        docker {
            image 'docker'
        }
      }
    stages {
        stage('Build') { 
            steps {
               sh 'docker build -t java2 dockerfile'  
            }
        }
        stage('Tag and Upload ') { 
            steps {
                sh 'docker tag java2 kpratik217/jenkins-kub:1'

            }
        }
        stage('Deploy') { 
            steps {
                sh 'docker push kpratik217/jenkins-kub:1'
            }
        }
    }
}
